# DevTrack .env_sample — monorepo root (redirect)
#
# This file is retained as a placeholder. After the EPIC-SPLIT, env vars
# are owned by each codebase separately:
#
#   devtrack_client/.env_sample  — Go daemon, CLI, git-sage, connectors
#   devtrack_server/.env_sample  — Python AI pipeline, webhooks, admin UI
#
# Copy the relevant file to .env in each subdirectory and fill in your values.
