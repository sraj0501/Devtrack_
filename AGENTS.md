# Shared agent instructions

Before working in this repository, read `agent-memory/INDEX.md` and the linked records relevant to
the task. `agent-memory/` is the only canonical project-memory location.

Do not create or update durable project memory in `AGENTS.md`, `.claude/`, `.codex/`, `.agents/`,
`.cursor/`, `.github/`, `.gemini/`, `.windsurf/`, or user-level agent storage. This file is a
discovery adapter only. Update shared context in `agent-memory/`.
