# Copilot repository adapter

Read `agent-memory/INDEX.md` and the linked records relevant to the task before proposing changes.
`agent-memory/` is the only canonical project-memory location. Do not store durable project state
in `.github/` or any other agent-specific directory; update the shared memory instead.
